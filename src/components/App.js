import React, {Component} from 'react';

export class App extends Component {
  render() {
    return (
      <div className="App">
        <header className="App-header">
          <p>
            Skeleton offline-first app using GraphQL, Redux, React, CRA, redux-offline, redux-persist.
          </p>
        </header>
      </div>
    );
  }
}

